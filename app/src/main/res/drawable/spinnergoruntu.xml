<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid
        android:color="@android:color/transparent">
    </solid>

    <corners
        android:radius="25dp">
    </corners>

    <stroke
        android:width="1dp"
        android:color="#FFF">
    </stroke>
    <TextView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:gravity="left"
        android:textColor="#FFFFFF"
        android:padding="5dip"
        />
    <item android:name="android:paddingStart">0dp</item>
    <item android:name="android:paddingEnd">0dp</item>
    <item android:name="android:textColor">@color/white</item>
    <item android:name="android:textSize">12sp</item>
    <padding
        android:top="0dp"
        android:right="10dp"
        android:left="10dp"
        android:bottom="0dp">
    </padding>

</shape>